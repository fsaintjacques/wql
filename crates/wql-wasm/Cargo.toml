[package]
name    = "wql-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[features]
default = []
regex   = ["wql-runtime/regex"]    # off by default — binary size

[dependencies]
wql-runtime = { workspace = true }

[lints]
workspace = true
